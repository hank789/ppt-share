<div class="subnav">
<%= render_list :class => "nav nav-tabs" do |li|
  li << link_to(t("users.menu.profile"), user_path(@user.login.downcase))
  li << link_to(t("users.menu.slides"), slides_user_path(@user.login.downcase))
  li << link_to(t("users.menu.collections"), collections_user_path(@user.login.downcase))
  li << link_to(t("users.menu.likes"), likes_user_path(@user.login.downcase))
end %>
</div>
