<div class="subnav">
<%= render_list :class => "nav nav-tabs" do |li|
  li << link_to(t("users.menu.profile"), user_path(@user.login.downcase))
	li << link_to(content_tag(:i, "", :class => "icon-cloud") + " #{t("menu.my_workspace")}", workspace_user_path(@user.login.downcase))
	li << link_to(content_tag(:i, "", :class => "icon-upload-alt") + " #{t("users.menu.slides")}", slides_user_path(@user.login.downcase))
	li << link_to(content_tag(:i, "", :class => "icon-bookmark") + " #{t("users.menu.collections")}", collections_user_path(@user.login.downcase))
	li << link_to(content_tag(:i, "", :class => "icon-heart") + " #{t("users.menu.likes")}", likes_user_path(@user.login.downcase))
end %>
</div>
