<div id="upload_landing" class="group section-wrap upper" id="upper">
<div class="wrap group">
<div class="section-2 group">
	<%= simple_form_for Attach.new, :url => attachs_path, :html => { id:"uploadDropzone", :class => "dropzone" } do |f|  %>
	<% end %>
<script type="text/javascript">
$(function() {
	$('#upload_editor').hide();
	Dropzone.options.uploadDropzone = {
		//addRemoveLinks : true,
		maxFilesize: 20,
		maxFiles: 1,
		acceptedFiles: ".ppt,.pptx,.pdf",
		dictResponseError: 'Error uploading file!',
		init: function() {
			this.on("success", function(file, data) {
				if(data) {
					$('#slide_slide').val(data);
					var cclass = "icon-refresh icon-spin";
					} else var cclass = "icon-bolt";
				$('#upload_editor').show();
				$('#carousel_tag').html("<div class='document_thumb'><i class='" + cclass + "'></i></div>");
			});
		}
	};
})
</script>
</div>
</div>
</div>

