<% if @attach.nil? || @attach.photos.blank? %>
    <i class="<%= if @attach.nil?
                    'fa fa-bolt'
                  else
                    'fa fa-spinner fa-spin'
                  end %> icon-large">
    </i>
<% else %>
    <% @photos.each do |item| %>
        <%= image_tag item.image, :class => "superbox-img" %>
    <% end %>
<% end %>
